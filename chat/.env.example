# Session and Cookie Configuration for Cross-Domain Support
# =========================================================

# Session secret key (CHANGE IN PRODUCTION!)
SESSION_SECRET_KEY=your-secret-key-change-in-production-2024

# Cookie domain configuration
# Use leading dot for parent domain to share cookies across subdomains
# Example: .growbal.net allows cookies to be shared between:
# - staging.growbal.net (frontend)
# - chat.staging.growbal.net (backend/iframe)
COOKIE_DOMAIN=.growbal.net

# SameSite cookie attribute
# Options: "none" (cross-site), "lax" (some cross-site), "strict" (same-site only)
# For iframe embedding across subdomains, use "none" with HTTPS
COOKIE_SAMESITE=none

# Secure cookie flag (requires HTTPS when true)
# Must be true when COOKIE_SAMESITE=none
COOKIE_SECURE=true

# CORS Configuration
# ==================

# Comma-separated list of allowed origins
ALLOWED_ORIGINS=https://staging.growbal.net,https://growbal.net,https://chat.staging.growbal.net

# Enable strict CORS (use ALLOWED_ORIGINS) or allow all origins (*)
# Set to "true" for production, "false" for development
CORS_STRICT=true

# Other Required Environment Variables
# ====================================

# Anthropic API Key for AI functionality
ANTHROPIC_API_KEY=your-anthropic-api-key-here